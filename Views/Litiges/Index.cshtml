@model IEnumerable<Avengers.Models.Litige>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.User.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RaisonLitige)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateLitige)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.User.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RaisonLitige)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateLitige)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LitigeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.LitigeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LitigeID })
        </td>
    </tr>
}

</table>
